# 安全策略

## 支持范围

安全修复优先面向 `main` 分支最新版本。请勿在公开 Issue、截图、日志或提交信息中披露真实账号、密码、访问令牌或本地数据库内容。

## 报告漏洞

请通过仓库维护者的私密联系方式报告漏洞，并至少包含：

- 受影响版本与复现步骤；
- 预期与实际结果；
- 影响范围与必要的脱敏证据；
- 可行的修复建议（如有）。

收到报告后，维护者应在 7 个工作日内确认，并在修复发布前避免公开技术细节。

## 本地数据安全

- 密码仅保存于本机 SQLite 数据库，优先使用 Electron `safeStorage` 加密。
- 系统安全存储不可用时，应用会使用兼容性加密回退；此模式不应视为抵御本机高权限攻击者的安全边界。
- 发布前请使用受信任的 Windows 代码签名证书签名安装包，并在 GitHub Secrets 中保存证书材料。
